package com.sheilajnieto.myshoppinglistfirebase;/*
@author sheila j. nieto 
@version 0.1 2024 -01 - 27
*/

import com.sheilajnieto.myshoppinglistfirebase.models.ListClass;
import com.sheilajnieto.myshoppinglistfirebase.models.ProductList;

import java.util.ArrayList;

public class ProductListActivity {


    public void loadProductList() {

    }

}

    /*
    // Verificamos si el documento existe y contiene la información de productos.
                if (documentSnapshot.exists()) {
        // Obtenemos la información de productos del documento (de la categoría).
        ArrayList<ProductList> productListInCategory = documentSnapshot.toObject(ListClass.class).getProductsList();

        // Verifica si la lista de productos está vacía.
        if (productListInCategory != null && !productListInCategory.isEmpty()){
        // Hay productos, mostramos el RecyclerView con los productos.

     */

